# Public Theme Assets (master)

This folder is used by the frontend dev server (Vite) to serve theme assets while developing.

Assets are referenced as:

- `/theme/master/assets/<file>`

In production, assets are served from `sparti-cms/theme/master/assets` (and this folder acts as a fallback).
