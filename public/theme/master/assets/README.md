# Master theme assets

Static assets for the **Master Theme** preview.

- `hero.svg`: used inside the theme preview page (`/theme/master`).
- `preview.svg`: optional thumbnail referenced by `sparti-cms/theme/master/theme.json`.
