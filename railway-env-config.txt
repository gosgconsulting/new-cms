# Railway Environment Variables Configuration
# Copy and paste these variables into your Railway service environment settings
# Railway will automatically replace the ${{}} placeholders with actual values
# ALL SECRETS ARE STORED SECURELY ON RAILWAY - NO SECRETS IN CODE

# PostgreSQL Database Configuration (Auto-populated by Railway from Postgres-33yU service)
DATABASE_PUBLIC_URL="postgresql://${{Postgres-33yU.PGUSER}}:${{Postgres-33yU.POSTGRES_PASSWORD}}@${{Postgres-33yU.RAILWAY_TCP_PROXY_DOMAIN}}:${{Postgres-33yU.RAILWAY_TCP_PROXY_PORT}}/${{Postgres-33yU.PGDATABASE}}"
DATABASE_URL="postgresql://${{Postgres-33yU.PGUSER}}:${{Postgres-33yU.POSTGRES_PASSWORD}}@${{Postgres-33yU.RAILWAY_PRIVATE_DOMAIN}}:5432/${{Postgres-33yU.PGDATABASE}}"
PGDATA="/var/lib/postgresql/data/pgdata"
PGDATABASE="${{Postgres-33yU.POSTGRES_DB}}"
PGHOST="${{Postgres-33yU.RAILWAY_PRIVATE_DOMAIN}}"
PGPASSWORD="${{Postgres-33yU.POSTGRES_PASSWORD}}"
PGPORT="5432"
PGUSER="${{Postgres-33yU.POSTGRES_USER}}"
POSTGRES_DB="${{Postgres-33yU.POSTGRES_DB}}"
POSTGRES_PASSWORD="${{Postgres-33yU.POSTGRES_PASSWORD}}"
POSTGRES_USER="${{Postgres-33yU.POSTGRES_USER}}"
# PERN-Store compatibility variables
POSTGRES_HOST="${{Postgres-33yU.RAILWAY_PRIVATE_DOMAIN}}"
POSTGRES_DATABASE="${{Postgres-33yU.POSTGRES_DB}}"
POSTGRES_DATABASE_TEST="railway_test"
POSTGRES_PORT="5432"

# Server Configuration
PORT="${{PORT}}"
NODE_ENV="production"

# Frontend API Configuration
VITE_API_BASE_URL="https://cms.sparti.ai"
# PERN-Store compatibility - VITE_API_URL maps to VITE_API_BASE_URL
VITE_API_URL="https://cms.sparti.ai"

# Email Configuration (Resend) - Store actual key in Railway
RESEND_API_KEY="your-resend-api-key-here"
SMTP_HOST="smtp.resend.com"
SMTP_PORT="465"
SMTP_USER="resend"
SMTP_FROM_EMAIL="noreply@gosg.com"
# PERN-Store compatibility - SMTP_FROM maps to SMTP_FROM_EMAIL
SMTP_FROM="noreply@gosg.com"

# Google Cloud Translation API - Store actual key in Railway
GOOGLE_CLOUD_TRANSLATION_API_KEY="your-google-cloud-translation-api-key-here"

# API Keys for integrations - Store actual keys in Railway
GOOGLE_API_KEY="your-google-api-key-here"
OPENROUTER_API_KEY="your-openrouter-api-key-here"
ANTHROPIC_API_KEY="your-anthropic-api-key-here"
VITE_PERPLEXITY_API_KEY="your-perplexity-api-key-here"
# Stripe Configuration (PERN-Store compatibility) - Store actual key in Railway
STRIPE_SECRET_KEY="your-stripe-secret-key-here"

# Railway Deployment Configuration
RAILWAY_DEPLOYMENT_DRAINING_SECONDS="60"
RAILWAY_HEALTHCHECK_TIMEOUT_SEC="120"
SSL_CERT_DAYS="820"

# PERN-Store compatibility - Authentication secrets
# SECRET maps to JWT_SECRET (set separately or use same value)
SECRET="your-jwt-secret-key-here"
REFRESH_SECRET="your-refresh-token-secret-key-here"

# PERN-Store compatibility - Frontend variables (VITE_ prefix required)
VITE_GOOGLE_CLIENT_ID="your-google-oauth-client-id-here"
VITE_GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret-here"
VITE_STRIPE_PUB_KEY="your-stripe-publishable-key-here"

# Frontend environment variables (VITE_ prefix required for browser access)
# Store actual keys in Railway
VITE_RESEND_API_KEY="your-resend-api-key-here"
VITE_GOOGLE_API_KEY="your-google-api-key-here"
VITE_OPENROUTER_API_KEY="your-openrouter-api-key-here"
VITE_ANTHROPIC_API_KEY="your-anthropic-api-key-here"
VITE_SMTP_FROM_EMAIL="noreply@gosg.com"

# Instructions:
# 1. Copy each line above (without the # comments) into Railway dashboard
# 2. Replace "your-*-api-key-here" placeholders with actual keys in Railway
# 3. Railway will automatically replace ${{}} placeholders with actual values
# 4. The DATABASE_PUBLIC_URL and DATABASE_URL will be auto-generated from your PostgreSQL service
# 5. For local development, use: npm run env:sync (requires Railway CLI)