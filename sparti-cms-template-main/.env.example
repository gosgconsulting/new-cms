# Supabase Configuration (automatically configured by Lovable)
# No need to set these manually - they are injected automatically

# Railway deployment settings
RAILWAY_DEPLOYMENT_DRAINING_SECONDS="60"
SSL_CERT_DAYS="820"

# Multi-tenant deployment (Railway specific)
# Set this in Railway dashboard for each deployment
DEPLOY_TENANT_ID="client-a"

# Development settings
NODE_ENV="development"
