# Quick Copy & Paste - Railway Tenant Environment Variables
# ====================================================
# STEP 1: Find your PostgreSQL service name in Railway Dashboard
#   - Go to your Railway project
#   - Look at your PostgreSQL service name (e.g., "Postgres-33yU", "Postgres-abc123", etc.)
#   - Replace "YOUR_POSTGRES_SERVICE_NAME" below with that exact name
#
# STEP 2: Replace "tenant-gosg" with your actual tenant slug
#
# STEP 3: Copy all lines below (without # comments) into Railway → Variables

CMS_TENANT="tenant-gosg"
DATABASE_PUBLIC_URL="${{YOUR_POSTGRES_SERVICE_NAME.DATABASE_PUBLIC_URL}}"
DATABASE_URL="${{YOUR_POSTGRES_SERVICE_NAME.DATABASE_URL}}"
NODE_ENV="production"
VITE_BACKEND_SERVER_URL="https://${{RAILWAY_PUBLIC_DOMAIN}}"
VITE_USE_BACKEND_SERVER="true"
BACKEND_PORT="${{PORT}}"
PORT="${{PORT}}"
RESEND_API_KEY="your-resend-api-key-here"
SMTP_FROM_EMAIL="noreply@gosg.com"
VITE_RESEND_API_KEY="your-resend-api-key-here"
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

