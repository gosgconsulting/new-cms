# Nixpacks config for Railway (no Dockerfile). Node 20, then install → build:production → start.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:production"]

[start]
cmd = "node scripts/start-production.js"
