# Static assets (build produces dist on Vercel; do not ignore dist or static hosting breaks)
public/

# Docs and server tests (not needed for build; scripts/ is required for build:vercel)
docs/
server/tests/

# Test and spec files
**/*.test.js
**/*.spec.js
**/*.test.ts
**/*.spec.ts

# Theme folders: excluded from upload. Build uses src/theme-stubs when VERCEL=1 or VITE_SKIP_THEMES=1 so all themes (including sparti-seo-landing) are stubbed and build succeeds without theme folder.
sparti-cms/theme/
public/theme/
