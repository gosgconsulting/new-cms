# Static assets (build produces dist on Vercel; do not ignore dist or static hosting breaks)
public/

# Docs and server tests (not needed for build; scripts/ is required for build:api)
docs/
server/tests/

# Test and spec files
**/*.test.js
**/*.spec.js
**/*.test.ts
**/*.spec.ts

# Theme folders: excluded from deployment for CMS (theme served from Blob via BLOB_THEME_MANIFEST_URL).
# Tenants deploying with the theme folder on disk can do so in their own environment (e.g. self-hosted or by changing ignore rules).
sparti-cms/theme/
public/theme/
