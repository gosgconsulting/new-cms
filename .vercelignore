# Static assets (build produces dist on Vercel; do not ignore dist or static hosting breaks)
public/

# Docs and server tests (not needed for build; scripts/ is required for build:api)
docs/
server/tests/

# Test and spec files
**/*.test.js
**/*.spec.js
**/*.test.ts
**/*.spec.ts

# Theme folders: exclude from deployment for CMS-only (theme served from Blob).
# Set EXCLUDE_THEME_FROM_DEPLOYMENT=1 on Vercel and use BLOB_THEME_MANIFEST_URL.
sparti-cms/theme/
public/theme/
