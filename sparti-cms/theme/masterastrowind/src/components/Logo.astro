---
import { SITE } from 'astrowind:config';

export interface Props {
  logoSrc?: string | null;
  siteName?: string | null;
}

const { logoSrc, siteName } = Astro.props;
const displayName = siteName || SITE?.name || 'Master Astrowind';
---

{logoSrc ? (
  <img
    src={logoSrc}
    alt={displayName}
    class="self-center h-8 w-auto ml-2 rtl:ml-0 rtl:mr-2"
  />
) : (
  <span
    class="self-center ml-2 rtl:ml-0 rtl:mr-2 text-2xl md:text-xl font-bold text-gray-900 whitespace-nowrap dark:text-white"
  >
    🚀 {displayName}
  </span>
)}
