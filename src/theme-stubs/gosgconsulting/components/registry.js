/**
 * Stub for gosgconsulting/components/registry when theme is excluded (VITE_SKIP_THEMES / Vercel CMS-only).
 * VisualEditorRenderer receives an empty registry so the build and runtime do not depend on the real theme.
 */
export const componentRegistry = {};
