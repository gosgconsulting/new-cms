# Railway Environment Variables for PostgreSQL Integration
# Copy and paste these into your Railway service settings

# Database Configuration (Railway will auto-populate these from your PostgreSQL service)
DATABASE_PUBLIC_URL=${{Postgres.DATABASE_PUBLIC_URL}}
DATABASE_URL=${{Postgres.DATABASE_URL}}
POSTGRES_DB=${{Postgres.POSTGRES_DB}}
POSTGRES_USER=${{Postgres.POSTGRES_USER}}
POSTGRES_PASSWORD=${{Postgres.POSTGRES_PASSWORD}}

# Server Configuration
PORT=${{PORT}}
NODE_ENV=production

# Railway Deployment Configuration
RAILWAY_HEALTHCHECK_TIMEOUT_SEC=120
